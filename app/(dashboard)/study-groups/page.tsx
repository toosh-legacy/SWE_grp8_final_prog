import DashboardPlaceholder from '@/components/DashboardPlaceholder';

export default function StudyGroupsPage() {
  return (
    <DashboardPlaceholder
      title="Study Groups"
      description="Browse and join study groups. This section will connect to groups and memberships when that backend work is wired up."
    />
  );
}
