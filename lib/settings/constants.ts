export const SETTINGS_STORAGE_KEY = "study-app-settings";

/** Rough cap so localStorage stays reasonable (~350KB base64) */
export const MAX_IMAGE_DATA_URL_LENGTH = 450_000;

/** Demo verification: any 6-digit code succeeds (no real email) */
export const EMAIL_VERIFICATION_CODE_LENGTH = 6;
