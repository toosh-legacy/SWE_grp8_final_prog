/**
 * Msg.ts — Campus Connect: Core
 * 
 * Generic message wrapper for API responses.
 * Used to wrap success/status messages from service operations.
 */

export class Msg {
  constructor(public message: string) {}
}
